#!/bin/bash

echo "plese enter the pin number"
read -s PIN_NUMBER # Here PIN_NUMBER is a variable that will store the user input. The -s option is used to hide the input while typing.    

echo "PIN Entered is $PIN_NUMBER" # Here we are using the variable PIN_NUMBER to display the entered pin number.

